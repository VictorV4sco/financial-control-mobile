import { ComingSoonScreen } from '@/features/coming-soon';

export default function SettingsTabScreen() {
  return (
    <ComingSoonScreen
      title="Settings"
      description="This tab is reserved for app preferences, profile options, and environment controls."
    />
  );
}
